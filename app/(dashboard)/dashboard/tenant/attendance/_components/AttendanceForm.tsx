'use client';

import type { Enrollment } from '@/lib/api/types';
import { useActionState } from 'react';
import { createAttendance, type AttendanceActionResponse } from '../_actions/actions';

export function AttendanceForm({ enrollments }: { enrollments: Enrollment[] }) { const [state, action, pending] = useActionState(createAttendance, { success: false, message: '' } satisfies AttendanceActionResponse); return <form action={action} className="space-y-4 rounded-xl border border-gray-200 bg-white p-5">{state.message && <p role="status" className={state.success ? 'rounded-lg bg-emerald-50 p-3 text-sm text-emerald-800' : 'rounded-lg bg-red-50 p-3 text-sm text-red-800'}>{state.message}</p>}<label className="block text-sm font-medium">Enrollment<select name="enrollment_id" required className="mt-1 min-h-11 w-full rounded-lg border border-gray-300 px-3"><option value="">Pilih enrollment</option>{enrollments.map((item) => <option key={item.id} value={item.id}>{item.student?.full_name || item.student_id} · {item.class?.name || item.class_id}</option>)}</select></label><label className="block text-sm font-medium">Schedule ID<input name="schedule_id" required className="mt-1 min-h-11 w-full rounded-lg border border-gray-300 px-3" /></label><label className="block text-sm font-medium">Tanggal<input name="date" type="date" required className="mt-1 min-h-11 w-full rounded-lg border border-gray-300 px-3" /></label><label className="block text-sm font-medium">Status<select name="status" defaultValue="present" className="mt-1 min-h-11 w-full rounded-lg border border-gray-300 px-3"><option value="present">Present</option><option value="absent">Absent</option><option value="late">Late</option><option value="excused">Excused</option></select></label><button disabled={pending} className="min-h-11 rounded-lg bg-blue-600 px-4 text-sm font-semibold text-white disabled:opacity-50">{pending ? 'Menyimpan...' : 'Catat attendance'}</button></form>; }