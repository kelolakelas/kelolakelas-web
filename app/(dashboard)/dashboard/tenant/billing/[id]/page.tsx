import Link from 'next/link';
import { getTransaction } from '../_queries/queries';

export default async function BillingTransactionDetailPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const result = await getTransaction(id); return <main className="mx-auto w-full max-w-3xl space-y-6"><Link href="/dashboard/tenant/billing" className="text-sm font-semibold text-blue-700">Kembali ke billing</Link>{result.error && <div role="alert" className="rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{result.error}</div>}{result.data ? <section className="space-y-3 rounded-xl border border-gray-200 bg-white p-6"><h1 className="text-2xl font-bold">Transaction detail</h1><p>Status: {result.data.status}</p><p>Gross amount: {result.data.currency || 'IDR'} {result.data.gross_amount.toLocaleString('id-ID')}</p><p>Payment method: {result.data.payment_method || 'Tidak tersedia'}</p><p>Payment intent: {result.data.payment_intent_id || 'Tidak tersedia'}</p><p>Paid at: {result.data.paid_at || 'Belum dibayar'}</p>{result.data.checkout_session_url && <a href={result.data.checkout_session_url} className="inline-flex min-h-11 items-center rounded-lg bg-blue-600 px-4 text-sm font-semibold text-white">Buka checkout</a>}</section> : <div className="rounded-xl border border-gray-200 bg-white p-6">Transaksi tidak ditemukan.</div>}</main>; }