'use client';
import { useRouter } from 'next/navigation';
import { useActionState, useEffect } from 'react';
import { registerInvitation, type InvitationActionResponse } from '../_actions/actions';
export function RegistrationForm({ token }: { token: string }) { const router = useRouter(); const [state, action, pending] = useActionState(registerInvitation, { success: false, message: '' } satisfies InvitationActionResponse); useEffect(() => { if (state.success) router.push('/login'); }, [state.success, router]); return <form action={action} className="space-y-4 rounded-xl border border-gray-200 bg-white p-6"><input type="hidden" name="token" value={token} />{state.message && <p role="status" className={state.success ? 'text-sm text-emerald-700' : 'text-sm text-red-700'}>{state.message}</p>}<input name="first_name" required placeholder="Nama depan" className="min-h-11 w-full rounded-lg border border-gray-300 px-3" /><input name="last_name" required placeholder="Nama belakang" className="min-h-11 w-full rounded-lg border border-gray-300 px-3" /><input name="password" type="password" minLength={6} required placeholder="Password" className="min-h-11 w-full rounded-lg border border-gray-300 px-3" /><button disabled={pending} className="min-h-11 rounded-lg bg-blue-600 px-4 text-sm font-semibold text-white">{pending ? 'Mendaftarkan...' : 'Daftar'}</button></form>; }